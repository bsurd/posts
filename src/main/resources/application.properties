spring.h2.console.enabled=true
spring.h2.console.path=/h2
spring.datasource.url=jdbc:h2:mem:memDB
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
server.port=8090
spring.devtools.restart.additional-paths=*

#init schema for h2 using the properties
#create mysql server locally and save the db there
#add dummy user
#see what mapstruct is
#how to configure datasources in a config package with an annotation and lookup @primary annotation
#Create unit tests
#look up layered arhitecture for springboot
#add logs from controller to db(use annotations)
#use @valid annotation
#create annotation that does a validation(has a constraint) @constraint, validate what fields should be on a post/comment
#lookup qualifier and primary spring, add value
